import React from 'react';

const MainButton = () => {
    return (
        <div>
            <button class="btn btn-secondary text-white bg-gradient-to-r from-secondary to-primary">Get Started</button>
        </div>
    );
};

export default MainButton;